# ملخص نظام الحوكمة - بالعربية
## Governance System Summary - In Arabic

---

## ✅ ما تم إنجازه | What Was Completed

### 1. ✅ إصلاح التحذيرات | Fixed Warnings

- ✅ **PosterController.java:** تم إصلاح ErrorResponse بإضافة @Getter
- ✅ **UserProfileService.java:** تم إصلاح null safety باستخدام Optional بشكل صحيح

---

### 2. ✅ نظام الحوكمة | Governance System

#### الحالات (Status):
- ✅ **DRAFT** - مسودة (يمكن التعديل)
- ✅ **PENDING** - في الانتظار (ينتظر الموافقة)
- ✅ **APPROVED** - معتمد (جاهز للطباعة بجودة 300 DPI)

#### المميزات:
- ✅ فقط المدير (Admin) يمكنه الموافقة
- ✅ الصورة بجودة 300 DPI متاحة فقط بعد الموافقة
- ✅ **logo.jpg** موجود تلقائياً في كل بوستر

---

### 3. ✅ الإشعارات | Notifications

- ✅ إشعارات تلقائية عند إرسال بوستر جديد
- ✅ قائمة بالإشعارات غير المقروءة
- ✅ تتبع الإشعارات المقروءة

---

### 4. ✅ نقاط النهاية | Endpoints

#### للمستخدمين:
- `POST /api/posters/generate` - إنشاء بوستر (DRAFT)
- `POST /api/posters/{posterId}/submit` - إرسال للموافقة (PENDING)

#### للمدير:
- `GET /api/admin/governance/pending` - رؤية البوسترات في الانتظار
- `POST /api/admin/governance/approve/{posterId}` - الموافقة على بوستر
- `GET /api/admin/governance/notifications` - رؤية الإشعارات
- `GET /api/admin/governance/posters/{posterId}/download` - تحميل البوستر المعتمد

---

## 📋 كيفية رؤية البوسترات في الانتظار | How to See Pending Posters

### الطريقة البسيطة:

**افتح الرابط في المتصفح:**
```
http://localhost:8080/api/admin/governance/pending
```

**أو على Railway:**
```
https://your-app.railway.app/api/admin/governance/pending
```

**النتيجة:** ستظهر قائمة بجميع البوسترات التي تنتظر الموافقة

---

### مثال الاستجابة:

```json
{
  "count": 5,
  "posters": [
    {
      "id": "poster-123",
      "status": "PENDING",
      "title": "نظافة الأسنان",
      "healthCenterId": "hc_center_01",
      "mohApproved": true,
      "createdAt": "2025-01-15T10:30:00"
    }
  ],
  "message": "البوسترات في انتظار الموافقة"
}
```

---

## ✅ الموافقة على بوستر | Approving a Poster

**الرابط:**
```
POST /api/admin/governance/approve/{posterId}
```

**مثال:**
```bash
curl -X POST http://localhost:8080/api/admin/governance/approve/poster-123 \
  -H "X-Admin-Id: admin"
```

**النتيجة:**
- البوستر يصبح **APPROVED**
- الصورة بجودة **300 DPI** متاحة للتحميل
- جاهز للطباعة في "AZAW TEAM CENTER"

---

## 🔔 الإشعارات | Notifications

**رؤية الإشعارات:**
```
GET /api/admin/governance/notifications
```

**رؤية الإشعارات غير المقروءة فقط:**
```
GET /api/admin/governance/notifications?unreadOnly=true
```

---

## 📥 تحميل البوستر المعتمد | Download Approved Poster

**بعد الموافقة:**
```
GET /api/admin/governance/posters/{posterId}/download
```

**النتيجة:**
- صورة بجودة **300 DPI**
- تحتوي على **logo.jpg** في الرأس
- جاهزة للطباعة

---

## 🎯 الخطوات السريعة | Quick Steps

1. **رؤية البوسترات في الانتظار:**
   ```
   GET /api/admin/governance/pending
   ```

2. **الموافقة على بوستر:**
   ```
   POST /api/admin/governance/approve/{posterId}
   ```

3. **تحميل البوستر المعتمد:**
   ```
   GET /api/admin/governance/posters/{posterId}/download
   ```

---

## ✨ الخلاصة | Summary

**جميع التحسينات مكتملة!**

**All improvements completed!**

- ✅ التحذيرات تم إصلاحها
- ✅ نظام الحوكمة جاهز
- ✅ الإشعارات تعمل
- ✅ **logo.jpg** متكامل تلقائياً

**المنصة جاهزة للإنتاج!**

**Platform is ready for production!**

---

**تم التطوير لدائرة صحة كركوك – قطاع كركوك الأول**  
**Developed for Kirkuk Health Directorate - First Sector**

