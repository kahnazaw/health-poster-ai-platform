# دليل إضافة المراكز الصحية الـ 23
## Guide: Adding 23 Health Centers

---

## 📋 نظرة عامة | Overview

هذا الدليل يشرح كيفية إضافة بيانات المراكز الصحية الـ 23 لدائرة صحة كركوك – قطاع كركوك الأول.

This guide explains how to add data for the 23 health centers for Kirkuk Health Directorate - First Sector.

---

## 🎯 الطريقتان | Two Methods

يمكنك إضافة بيانات المراكز الصحية بطريقتين:

You can add health center data in two ways:

### الطريقة 1: من خلال قاعدة البيانات (SQL) | Method 1: Through Database (SQL)
### الطريقة 2: من خلال واجهة API | Method 2: Through API

---

## 📊 الطريقة 1: قاعدة البيانات (SQL) | Method 1: Database (SQL)

### الخطوة 1: إنشاء الجدول | Step 1: Create Table

افتح قاعدة البيانات PostgreSQL وقم بتشغيل:

```sql
CREATE TABLE IF NOT EXISTS user_profiles (
    id VARCHAR(255) PRIMARY KEY,
    user_id VARCHAR(255) UNIQUE NOT NULL,
    health_center_name VARCHAR(200),
    manager_name VARCHAR(150),
    directorate_name VARCHAR(200) DEFAULT 'دائرة صحة كركوك – قطاع كركوك الأول',
    logo_path VARCHAR(500) DEFAULT 'logo.jpg',
    show_verification_badge BOOLEAN DEFAULT true,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### الخطوة 2: إضافة بيانات المراكز | Step 2: Add Center Data

افتح ملف: `java-backend/src/main/resources/db/seed/seed_23_health_centers.sql`

**استبدل `user_id_01` إلى `user_id_23` بمعرفات المستخدمين الفعلية من جدول المستخدمين.**

مثال:

```sql
INSERT INTO user_profiles (id, user_id, health_center_name, manager_name, directorate_name, logo_path, show_verification_badge)
VALUES
    (gen_random_uuid()::text, 'user_actual_id_1', 'المركز الصحي الأول', 'د. أحمد محمد علي', 'دائرة صحة كركوك – قطاع كركوك الأول', 'logo.jpg', true),
    (gen_random_uuid()::text, 'user_actual_id_2', 'المركز الصحي الثاني', 'د. فاطمة حسن', 'دائرة صحة كركوك – قطاع كركوك الأول', 'logo.jpg', true),
    -- ... إلخ
```

### الخطوة 3: تشغيل SQL | Step 3: Run SQL

قم بتشغيل ملف SQL في قاعدة البيانات.

---

## 🌐 الطريقة 2: واجهة API | Method 2: API

### الخطوة 1: إضافة مركز صحي واحد | Step 1: Add One Health Center

استخدم هذا الأمر:

```bash
curl -X POST http://localhost:8080/api/health-centers \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "user_actual_id_1",
    "healthCenterName": "المركز الصحي الأول",
    "managerName": "د. أحمد محمد علي"
  }'
```

### الخطوة 2: تحديث مركز صحي | Step 2: Update Health Center

```bash
curl -X PUT http://localhost:8080/api/health-centers/user_actual_id_1 \
  -H "Content-Type: application/json" \
  -d '{
    "healthCenterName": "المركز الصحي الأول",
    "managerName": "د. أحمد محمد علي"
  }'
```

### الخطوة 3: الحصول على بيانات مركز | Step 3: Get Center Data

```bash
curl http://localhost:8080/api/health-centers/user_actual_id_1
```

---

## 📝 مثال كامل: إضافة جميع المراكز الـ 23 | Complete Example: Add All 23 Centers

### باستخدام API (Bash Script):

```bash
#!/bin/bash

# Center 1
curl -X POST http://localhost:8080/api/health-centers \
  -H "Content-Type: application/json" \
  -d '{"userId": "user_1", "healthCenterName": "المركز الصحي الأول", "managerName": "د. أحمد محمد علي"}'

# Center 2
curl -X POST http://localhost:8080/api/health-centers \
  -H "Content-Type: application/json" \
  -d '{"userId": "user_2", "healthCenterName": "المركز الصحي الثاني", "managerName": "د. فاطمة حسن"}'

# ... إلخ للـ 23 مركز
```

### باستخدام SQL:

```sql
-- إضافة جميع المراكز دفعة واحدة
INSERT INTO user_profiles (id, user_id, health_center_name, manager_name, directorate_name, logo_path, show_verification_badge)
VALUES
    (gen_random_uuid()::text, 'user_1', 'المركز الصحي الأول', 'د. أحمد محمد علي', 'دائرة صحة كركوك – قطاع كركوك الأول', 'logo.jpg', true),
    (gen_random_uuid()::text, 'user_2', 'المركز الصحي الثاني', 'د. فاطمة حسن', 'دائرة صحة كركوك – قطاع كركوك الأول', 'logo.jpg', true),
    -- ... إضافة باقي المراكز
ON CONFLICT (user_id) DO UPDATE
SET health_center_name = EXCLUDED.health_center_name,
    manager_name = EXCLUDED.manager_name,
    updated_at = CURRENT_TIMESTAMP;
```

---

## ✅ التحقق | Verification

### التحقق من قاعدة البيانات:

```sql
SELECT 
    user_id,
    health_center_name,
    manager_name,
    directorate_name
FROM user_profiles
ORDER BY health_center_name;
```

يجب أن ترى 23 صفاً (مركز صحي).

### التحقق من API:

```bash
curl http://localhost:8080/api/health-centers
```

---

## 📋 البيانات المطلوبة لكل مركز | Required Data Per Center

لكل مركز صحي، تحتاج إلى:

For each health center, you need:

1. **user_id** - معرف المستخدم (من جدول المستخدمين)
2. **health_center_name** - اسم المركز الصحي
3. **manager_name** - اسم مدير وحدة تعزيز الصحة

**ملاحظة:** باقي الحقول (directorate_name, logo_path) تُضبط تلقائياً.

---

## 🎯 مثال عملي | Practical Example

### المركز الصحي الأول:

**البيانات:**
- user_id: `hc_center_01`
- health_center_name: `المركز الصحي الأول`
- manager_name: `د. أحمد محمد علي`

**باستخدام API:**

```bash
curl -X POST http://localhost:8080/api/health-centers \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "hc_center_01",
    "healthCenterName": "المركز الصحي الأول",
    "managerName": "د. أحمد محمد علي"
  }'
```

**باستخدام SQL:**

```sql
INSERT INTO user_profiles (id, user_id, health_center_name, manager_name, directorate_name, logo_path, show_verification_badge)
VALUES (
    gen_random_uuid()::text,
    'hc_center_01',
    'المركز الصحي الأول',
    'د. أحمد محمد علي',
    'دائرة صحة كركوك – قطاع كركوك الأول',
    'logo.jpg',
    true
);
```

---

## 🔄 تحديث بيانات مركز موجود | Update Existing Center

### باستخدام API:

```bash
curl -X PUT http://localhost:8080/api/health-centers/hc_center_01 \
  -H "Content-Type: application/json" \
  -d '{
    "healthCenterName": "المركز الصحي الأول - المحدث",
    "managerName": "د. أحمد محمد علي - المحدث"
  }'
```

### باستخدام SQL:

```sql
UPDATE user_profiles
SET 
    health_center_name = 'المركز الصحي الأول - المحدث',
    manager_name = 'د. أحمد محمد علي - المحدث',
    updated_at = CURRENT_TIMESTAMP
WHERE user_id = 'hc_center_01';
```

---

## 📞 للمساعدة | For Help

إذا واجهت أي مشكلة:

1. تحقق من أن الجدول `user_profiles` موجود
2. تحقق من أن `user_id` موجود في جدول المستخدمين
3. تحقق من التطبيق يعمل: `http://localhost:8080/api/health-centers`

---

## ✨ الخلاصة | Summary

**الطريقة الموصى بها:** استخدام SQL لإضافة جميع المراكز دفعة واحدة.

**Recommended Method:** Use SQL to add all centers at once.

**بعد الإضافة:** النظام سيعرض تلقائياً اسم المركز واسم المدير في تذييل كل بوستر!

**After Adding:** The system will automatically display the center name and manager name in the footer of each poster!

---

**تم التطوير لدائرة صحة كركوك – قطاع كركوك الأول**  
**Developed for Kirkuk Health Directorate - First Sector**

