# دليل الموافقة على البوسترات - بالعربية
## Poster Approval Guide - In Arabic

---

## 🎯 نظرة عامة | Overview

هذا الدليل يشرح كيفية رؤية قائمة البوسترات التي تنتظر موافقتك كمدير قطاع.

This guide explains how to see the list of posters waiting for your approval as Sector Manager.

---

## 📋 كيفية رؤية البوسترات في الانتظار | How to See Pending Posters

### الطريقة 1: من المتصفح | Method 1: From Browser

**افتح الرابط:**
```
http://localhost:8080/api/admin/governance/pending
```

**أو على Railway:**
```
https://your-app.railway.app/api/admin/governance/pending
```

**النتيجة:** ستظهر قائمة بجميع البوسترات التي تنتظر الموافقة

---

### الطريقة 2: باستخدام curl | Method 2: Using curl

```bash
curl http://localhost:8080/api/admin/governance/pending
```

---

## 📊 مثال الاستجابة | Example Response

```json
{
  "count": 5,
  "posters": [
    {
      "id": "poster-123",
      "status": "PENDING",
      "userId": "hc_center_01",
      "healthCenterId": "hc_center_01",
      "title": "نظافة الأسنان",
      "topic": "نظافة الأسنان",
      "mohApproved": true,
      "createdAt": "2025-01-15T10:30:00"
    },
    {
      "id": "poster-124",
      "status": "PENDING",
      "userId": "hc_center_02",
      "healthCenterId": "hc_center_02",
      "title": "التغذية الصحية",
      "topic": "التغذية الصحية",
      "mohApproved": true,
      "createdAt": "2025-01-15T11:00:00"
    }
  ],
  "message": "البوسترات في انتظار الموافقة"
}
```

---

## ✅ الموافقة على بوستر | Approving a Poster

### من المتصفح:

**افتح الرابط:**
```
POST http://localhost:8080/api/admin/governance/approve/{posterId}
```

**مثال:**
```
POST http://localhost:8080/api/admin/governance/approve/poster-123
```

### باستخدام curl:

```bash
curl -X POST http://localhost:8080/api/admin/governance/approve/poster-123 \
  -H "X-Admin-Id: admin"
```

**النتيجة:**
```json
{
  "posterId": "poster-123",
  "status": "APPROVED",
  "message": "تمت الموافقة على البوستر بنجاح - جاهز للطباعة بجودة 300 DPI",
  "approvedAt": "2025-01-15T12:00:00"
}
```

---

## 🔔 الإشعارات | Notifications

### رؤية الإشعارات الجديدة:

**الرابط:**
```
GET http://localhost:8080/api/admin/governance/notifications
```

**لرؤية الإشعارات غير المقروءة فقط:**
```
GET http://localhost:8080/api/admin/governance/notifications?unreadOnly=true
```

**مثال الاستجابة:**
```json
{
  "unreadCount": 3,
  "notifications": [
    {
      "id": "notif-1",
      "type": "POSTER_SUBMITTED",
      "posterId": "poster-123",
      "healthCenterName": "المركز الصحي الأول",
      "message": "بوستر جديد في انتظار الموافقة من: المركز الصحي الأول - نظافة الأسنان",
      "read": false,
      "createdAt": "2025-01-15T10:30:00"
    }
  ],
  "message": "الإشعارات"
}
```

---

## 📥 تحميل البوستر المعتمد | Download Approved Poster

### بعد الموافقة:

**الرابط:**
```
GET http://localhost:8080/api/admin/governance/posters/{posterId}/download
```

**مثال:**
```
GET http://localhost:8080/api/admin/governance/posters/poster-123/download
```

**النتيجة:**
- سيتم تحميل الصورة بجودة **300 DPI**
- جاهزة للطباعة في "AZAW TEAM CENTER"
- تحتوي على **logo.jpg** في الرأس

---

## 🔄 سير العمل | Workflow

### 1. إنشاء البوستر (DRAFT):
```
POST /api/posters/generate
```
- البوستر يُنشأ بحالة **DRAFT**
- يمكن التعديل

### 2. إرسال للموافقة (PENDING):
```
POST /api/posters/{posterId}/submit
```
- البوستر يصبح **PENDING**
- يظهر في قائمة الانتظار
- يتم إنشاء إشعار للمدير

### 3. الموافقة (APPROVED):
```
POST /api/admin/governance/approve/{posterId}
```
- البوستر يصبح **APPROVED**
- الصورة بجودة **300 DPI** متاحة للتحميل
- جاهز للطباعة

---

## 📝 ملاحظات مهمة | Important Notes

1. **فقط المدير يمكنه الموافقة:**
   - فقط المستخدم بصلاحية Admin يمكنه الموافقة
   - استخدم `X-Admin-Id` في Header

2. **جودة 300 DPI:**
   - الصورة بجودة 300 DPI متاحة فقط بعد الموافقة
   - جاهزة للطباعة في "AZAW TEAM CENTER"

3. **الشعار:**
   - **logo.jpg** موجود تلقائياً في كل بوستر
   - لا حاجة لإضافته يدوياً

4. **الإشعارات:**
   - يتم إنشاء إشعار تلقائياً عند إرسال بوستر جديد
   - يمكنك رؤية عدد الإشعارات غير المقروءة

---

## 🎯 الخطوات السريعة | Quick Steps

### لرؤية البوسترات في الانتظار:

1. **افتح الرابط:**
   ```
   http://localhost:8080/api/admin/governance/pending
   ```

2. **راجع القائمة:**
   - ستظهر جميع البوسترات في حالة PENDING
   - مع معلومات المركز الصحي والعنوان

3. **وافق على البوستر:**
   ```
   POST /api/admin/governance/approve/{posterId}
   ```

4. **حمّل البوستر المعتمد:**
   ```
   GET /api/admin/governance/posters/{posterId}/download
   ```

---

## ✨ الخلاصة | Summary

**لرؤية البوسترات في الانتظار:**

**افتح:**
```
GET /api/admin/governance/pending
```

**للموافقة:**
```
POST /api/admin/governance/approve/{posterId}
```

**للتحميل:**
```
GET /api/admin/governance/posters/{posterId}/download
```

**بسيط وسهل!** 🎉

---

**تم التطوير لدائرة صحة كركوك – قطاع كركوك الأول**  
**Developed for Kirkuk Health Directorate - First Sector**

